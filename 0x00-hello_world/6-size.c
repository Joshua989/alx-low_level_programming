#include <stdio.h>
/**
 * main - Entry
 * Return: Always 0
 */
int main(void)
{
	printf("size of a chat : %i byte(s)\n", sizeo f(char));
		printf("size of a chat : %i byte(s)\n", sizeo f(int));
			printf("size of a chat : %i byte(s)\n", sizeo f(float));
				return  (0);
